{{#if named}}
{{#each files}}
import { {{camelCaseBasename this}} } from './{{basename this}}'
{{/each}}
{{else}}
{{#each files}}
import {{camelCaseBasename this}} from './{{basename this}}'
{{/each}}
{{/if}}

export {
{{#each files}}
  {{camelCaseBasename this}},
{{/each}}
}